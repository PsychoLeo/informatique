import sys

def construction_tableau(u, m) :
    T = []

def recherche_motif() :
    pass

def main() :
    input = sys.stdin.readline
    u = input().strip() # motif
    v = input().strip() # texte
    m = len(u)
    n = len(v)
    T = construction_tableau(u, m)
