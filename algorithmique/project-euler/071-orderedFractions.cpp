#include <cstdio>
#include <vector>
#include <algorithm>

using namespace std;

int main() {
    printf("%d\n", __gcd(428570, 999999));
    // By hand, and verifying that GCD(3*142857-1, 7*142857) = 1
}